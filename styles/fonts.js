import { StyleSheet } from 'react-native'

const fonts = StyleSheet.create({
    titleFont: {
        fontSize: 64,
        color: '#1F2226',
    },
    cardTitle: {
        fontSize: 48,
        color: '#1F2226',
    }
});

export default fonts;